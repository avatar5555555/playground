{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna exec npm install && lerna bootstrap",
    "dev": "lerna exec npm run dev"
  },
  "devDependencies": {
    "lerna": "^3.2.1"
  }
}
